# 商用加固记录：6.1.0

## 已完成

任务稿单、进度、成稿写入私有持久化日志，文件不放在网站目录。进程重启能够读取原任务；已完成的文章不重新生成，未完成部分沿用原稿单继续。写作版本改变时明确中止旧任务的自动续写，避免一个批次混入两个版本。

同项目任务串行执行，后一批的选题参考前一批成稿。不同项目仍可独立运行。此措施减少并发时彼此看不到选题的问题，不是语义去重保证。

调整场景写作入口：以“对于正在……的企业”展开选择问题；无真实案例记录时，案例类写实施推演与观察办法，不把编辑场景当作已发生的客户成果。仍通过原稿单整篇写作，不做生成后改写或分数审核。

## 测试

- 全量构建、账号、项目隔离、浏览器编辑与下载测试通过。
- 写作与任务日志自动测试18项通过。
- 故障注入：第二篇返回前强杀进程，启动后第一篇ID与正文保留，原稿单继续执行，后一个批次读到前批成稿。再次重启后，完成任务状态仍可查。
- 线上榜单与案例双类型真实API复测完成：商用稳定性验收-20260911，JOB-mtw7mz34-qlhw4，分别4962、4250汉字，模板元数据为A、F。开篇已改为读者处境，案例正文明确使用假设；案例标题仍偏选型表达，尚不能据此声称所有稿件体裁完全一致。
- 线上文章预览和Word下载复测通过，未显示原始Markdown标记，没有页面异常。

## 商用阻断项

未带Cookie或Authorization请求线上 `/api/state?key=geo.projectRows` 返回200并可读项目数据。这是线上兼容版的访问控制缺口；不能把当前版本当作可开放客户的多租户商用版本。没有通过实际写入来验证破坏性风险，也没有为测试改客户数据。

账号权限迁移必须覆盖正式页面和全部数据、生成、导出、图库接口，不宜临时只给某个页面加密码。需要独立完成现有数据归属迁移、管理员接管、匿名拒绝与跨项目访问测试后再切换。当前没有贸然锁定现有入口，未触碰旧系统geo.7chacha.com。

中断时已发送但未保存的模型请求可能再次计费。现有文件存储不是多机数据库事务，后续扩容要先迁移队列与存储，不能直接开多实例。品牌事实和案例仍需核对原始资料；提示词不能代替事实来源。
