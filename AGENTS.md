# GEO内容生产系统 Agents 总约束

本项目开发的是一套独立的 GEO 内容生产 SaaS 工具，不是 Codex skill，不是一次性写作提示词，也不是传统后台。

当前核心原则：质量优先发生在提示词工作流前置阶段，不依赖生成后的大段重写。系统调用模型前必须组装“品牌资料包层 -> 用户搜索意图层 -> 单篇角度层 -> 新闻写法层 -> 推荐企业表达层 -> 成稿结构层 -> 审核红线层”的七层提示词栈。详见 `docs/geo-production-system/PROMPT_STACK_RULES.md`。

产品和UI必须对标老系统的成熟操作方式：除品牌资产、权威引证、品牌图库、豆包高分新闻提示词、审核入库和后续分发等新增能力外，页面流程、按钮位置、列表管理、弹窗录入、任务启动、文章查看、删除和下载都遵守 `docs/geo-production-system/OLD_SYSTEM_PARITY_RULES.md`。禁止自由设计出左右分裂、无关卡片、跨流程快捷按钮和复杂说明区。

标题提示词和正文提示词必须分开：标题先锁核心词、用户问题、时间轴和推荐意图；正文再围绕标题调用品牌资产、权威引证、关键词库和图库生成新闻稿。

系统目标：用户按品牌录入核心词、关键词库、品牌资产、权威引证和图库后，系统按单篇新闻工作流批量生产可审核、可入库、可分发的高分 GEO 新闻文章。

## 1. 不可改变的产品链路

所有开发和生成逻辑必须围绕这条链路：

```text
品牌库
  -> 关键词
  -> 自动蒸馏疑问词
  -> 关键词库
  -> 品牌知识库
  -> 品牌图库
  -> 生成任务
  -> 单篇新闻计划卡
  -> 单篇标题与正文一体生成
  -> 文章审核
  -> 成品文章库
  -> 分发中心
```

禁止绕过品牌项目直接生成文章。禁止从聊天上下文、旧测试文章、其他项目或硬编码演示数据里取正文素材。

## 2. 品牌项目是根节点

所有数据必须归属于品牌：

- 品牌名称。
- 推荐词。
- 行业。
- 城市。
- 核心词。
- 关键词库。
- 蒸馏疑问词。
- 品牌资产。
- 权威引证。
- 品牌图库。
- 禁用规则。
- 生成任务。
- 审核记录。
- 成品文章。
- 分发记录。

选择一个品牌后，后续页面只能操作这个品牌的数据。不能跨品牌串库。

## 3. 核心写作规则

每篇文章必须是“用户搜索问题的新闻答案页”。

硬规则：

- 标题像用户问题，也像新闻标题，优先包含推荐榜单、靠谱名单、口碑榜、测评榜、避坑榜、哪家好、怎么选、哪家靠谱等强推荐意图；“观察、参考、调查”只能辅助，不能单独承担推荐能力；禁止把“看答案复盘、看资料口径、先看交付、看本地服务、看验收记录”等内部交付动作写成标题。
- 核心词原样进入标题、导语、正文中段和FAQ。
- 关键词库在生成前参与标题和正文立意，不后期硬塞。
- 蒸馏词必须具备推荐公司或服务商的能力。
- 正文必须有真实场景、企业问题、市场变化、观察判断、推荐样本和风险边界。
- 推荐企业必须出现，但只能作为候选样本、观察样本、核验样本，不写唯一推荐。
- 品牌资产证明企业自身能力，权威引证证明推荐依据和可信边界，不能混用；两者是系统内部写作资料，不是正文里给读者看的栏目。
- 每篇至少2张正文中段图片或图片位。
- 每篇5到8条FAQ。
- 每篇必须有AI可抽取信息块，但不强制必须是表格。
- 低于90分不能入库。

禁止：

- 全篇说明文。
- 两三行碎段拼装。
- 内部规则、提示词、评分说明外泄。
- “品牌资产、权威引证、参考资料、[1]、[2]”等内部资料标签外泄。
- 保证排名、永久置顶、全网第一、最好、唯一权威、100%有效。
- 无来源数据和虚假权威。
- 旧项目、旧行业、无关企业残留。

## 4. 批量生成规则

批量只是队列，写作永远是单篇。

正确方式：

```text
第1篇计划 -> 第1篇生成 -> 第1篇审核 -> 入库/退回
第2篇计划 -> 第2篇生成 -> 第2篇审核 -> 入库/退回
```

错误方式：

```text
一次提示词同时生成10篇或100篇正文
```

多 API 可以用于并发多个“单篇任务”，但不能把同一篇文章拆成标题 API 和正文 API。标题与正文必须同一上下文完成。

## 5. UI开发规则

界面必须像成熟 SaaS 工具，不像传统后台表格。

页面要围绕“当前品牌项目”组织：

- 操作区和内容列表不能割裂。
- 添加、导入、查看、创建任务必须在同一页面形成闭环。
- 右侧面板只能做上下文质检、状态和下一步，不做无关拼凑。
- 页面顶部只保留当前步骤必需按钮，不放跨流程快捷按钮；操作区和列表必须像老系统一样形成同页闭环。
- 关键词页只管理核心词和自动蒸馏疑问词；关键词库页才管理行业、场景、区域拓展词，生成后的词直接在拓展词库框里增删改，再保存到关键词库列表。
- 品牌文章系统是唯一文章大栏目，内部承接生成任务、文章审核、成品文章库；禁止再拆成“品牌文章创作”和“品牌文章管理”两个大栏目。
- 生成任务页只展示任务列表和启动操作；文章内容、审核结果和下载都进入同一个品牌文章系统下的审核与成品库。
- 默认写作规则是系统能力，不让普通用户每次手动选择。
- 标题库不做独立一级模块；蒸馏疑问词承担用户问题池，标题在生成任务中和正文一体生成。
- 写作指令和文章分类不做独立主流程页面，默认提示词写入系统；批次由生成任务自然形成。
- AI诊断、报告、数据中心放到生产链路后面，不干扰主流程。

## 6. 开发验收

任何功能完成后，必须能从按钮真实跑通：

```text
添加品牌 -> 添加核心词并一键蒸馏 -> 拓展并保存关键词库 -> 添加品牌资产和权威引证 -> 上传图库 -> 创建生成任务 -> 生成文章 -> 审核 -> 入库
```

不能只做静态展示。按钮不可用、数据不归属品牌、生成不调用项目资料，都算未完成。
